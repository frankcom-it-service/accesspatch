# Planned runtime configuration only. No AccessPatch product runtime exists.
# gpt-5.6-sol is the full model ID; gpt-5.6 is its current alias.
# Human-supervised project authentication and one minimal Responses API access
# test were verified on 2026-07-15. Product integration,
# schema validation, safety checks, audit logging, and fallback are NOT YET
# IMPLEMENTED. The real key remains outside this repository; never paste it here.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-sol

# Optional future local demo target; format and use are NOT YET IMPLEMENTED.
ACCESSPATCH_DEMO_BASE_URL=
